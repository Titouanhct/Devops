services:
  jenkins:
    build: .
    container_name: jenkins
    ports:
      - 8080:8080
    volumes:
      - jenkins_home:/var/jenkins_home
    env_file:
      - ./env-jenkins.env

volumes:
  jenkins_home: